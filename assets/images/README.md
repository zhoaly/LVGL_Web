# Image sources

Place original PNG/JPEG artwork in this directory. Large or replaceable images
should eventually be converted to LVGL `.bin` assets and loaded through the
project resource API rather than referenced directly from pages.

Every image must be declared in `assets/manifest.yaml` before it is used.
